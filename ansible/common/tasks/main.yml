#SPDX-License-Identifier: MIT-0
---
# tasks file for common
- name: Install common dependencies
  ansible.builtin.apt:
    name:
      - git
      - curl
      - wget
      - unzip
      - python3
      - python3-pip
      - jq
    state: present

